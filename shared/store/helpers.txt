Recommended for Your Case (Kazzy Enterprises)

You’re dealing with occasional updates (routes, places, shops)
and many users reading the same data, not writing constantly.

👉 So the best design is:

Hybrid caching + background auto-refresh

Implementation idea:

Store lastFetchedAt and lastUpdatedAt for each dataset.

When user loads data:

Use cached version instantly.

In background, do a light Firestore check (using a small query with orderBy('updatedAt', 'desc') limit(1)).

If Firestore’s updatedAt > local cache’s lastFetchedAt, re-fetch fresh data silently and update the store.